#include <stdio.h>

int main(void)
{
    int n;
    scanf("%i", &n);
    for (int i = 0; i < n; i++)
    {
        list[n];
        int a = list[1];
        int b = list[3];
        int num = 1;
        for (int j = b + 1; j < a + 1; j++)
        {
            num = num * j;
        }
        printf("%i", num);
    }
}